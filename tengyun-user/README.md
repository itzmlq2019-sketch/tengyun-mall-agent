## tengyun-user
用户中心模块，负责商城的账户体系维护及基于 JWT 的无状态身份验证。

### 核心技术点
- **JWT 身份鉴权系统**：设计并实现基于 JSON Web Token 的分布式鉴权机制，负责用户登录后的 Token 签发。
- **数据安全保障**：采用 BCrypt 强哈希算法对用户密码进行不可逆加密存储，从源头保障用户信息安全。
- **分布式认证集成**：与网关层深度配合，支持基于 `X-User-Id` 的请求头透传机制，为下游微服务提供受信的身份标识。
